#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	float r,A;
	printf("digita o raio q o parta nessa merda...:");
	scanf("%f",&r);
	A=pow(r,2)*3,14;
	printf("o tamanho do meu desprezo por voce....:%.2f",A);
	
	return 0;
}
